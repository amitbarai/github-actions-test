name: shell commands
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
        - name: echo "Hello world"
          run: echo "Hello world"
        - name: multiple scripts
          run: |
            node -v
            npm -v